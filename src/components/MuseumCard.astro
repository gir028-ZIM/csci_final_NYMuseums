---
interface Props {
  name: string;
  address1: string;
  city: string;
  zip: string;
  tel: string;
  url: string;
  image?: string;
}

const { name, address1, city, zip, tel, url, image } = Astro.props;
---

<div class="card">
  {image && (
    <div class="card-image">
      <img src={image} alt={name} />
    </div>
  )}
  <div class="card-header">
    <h3>{name}</h3>
  </div>
  <div class="card-body">
    <p class="address">
      <strong>{address1}</strong>
    </p>
    <p class="city-zip">{city}, {zip}</p>
    <p class="tel">
      <a href={`tel:${tel}`}>{tel}</a>
    </p>
  </div>
  <div class="card-footer">
    <a href={url} target="_blank" class="visit-btn">Visit Website</a>
  </div>
</div>

<style>
  .card {
    background: white;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    height: 100%;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .card:hover {
    transform: translateY(-4px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
  }

  .card-image {
    width: 100%;
    height: 200px;
    overflow: hidden;
    background-color: #f0f0f0;
  }

  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .card-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 16px;
    border-bottom: 2px solid #f0f0f0;
  }

  .card-header h3 {
    margin: 0;
    font-size: 1.1rem;
    line-height: 1.4;
  }

  .card-body {
    padding: 16px;
    flex-grow: 1;
  }

  .address {
    margin: 0 0 8px 0;
    color: #333;
    font-size: 0.95rem;
  }

  .city-zip {
    margin: 0 0 12px 0;
    color: #666;
    font-size: 0.9rem;
  }

  .tel {
    margin: 0;
    font-size: 0.9rem;
  }

  .tel a {
    color: #667eea;
    text-decoration: none;
  }

  .tel a:hover {
    text-decoration: underline;
  }

  .card-footer {
    padding: 12px 16px;
    border-top: 1px solid #f0f0f0;
  }

  .visit-btn {
    display: inline-block;
    background-color: #667eea;
    color: white;
    padding: 8px 16px;
    border-radius: 4px;
    text-decoration: none;
    font-size: 0.9rem;
    transition: background-color 0.2s;
  }

  .visit-btn:hover {
    background-color: #764ba2;
  }
</style>
